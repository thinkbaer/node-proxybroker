
app:
  name: proxybroker

logging:
  enable: true
  level: debug
  transports:
  - console: {}


storage:
  default:
    type: sqlite
    database: tmp/database.db
    synchronize: true



server:
  proxyserver:
    type: proxyserver
    enable: true
    timeout: 120000
    # limit of search repeats if proxy failed
    repeatLimit: 10


proxy-broker:

  validator:
    parallel: 100
#    schedule:
#      enable: true
    judge:
      selftest: true
      remote_lookup: true
      remote_ip: 127.0.0.1
      ip: 0.0.0.0
      request:
        timeout: 5000


  provider:
    startup: true
    parallel: 5
    schedule:
      enable: true



