
import {IRunnable} from "./irunnable";
import {SpecNode, Root, Visit} from "./provider_spec";
import EventEmitter = NodeJS.EventEmitter;

/*

export class FetchRunner extends EventEmitter implements IRunnable{

    spec: any


    constructor(spec : any){
        super()
        this.spec = spec
    }



    run() {
        return new ProcessNode(this.spec.op)
    }

}*/